PUID=1000
PGID=1000
TZ=Europe/Madrid

#
# Optional network/proxy settings (deprecated)
# This repo previously supported a GCP tinyproxy + SSH tunnel for Prowlarr egress.
# That setup has been removed as part of slimming down the stack.
